import 'package:flutter/material.dart';

class CoursesPanel extends StatelessWidget {
  const CoursesPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Column(children: [Text('Cursos')]));
  }
}
